name: remix-hono-template

services:
  node:
    container_name: node
    build:
      context: .
      dockerfile: ./docker/localhost/node/Dockerfile
    ports:
      - 80:3000
    tty: true
    volumes:
      - .:/app
    networks:
      - remix-hono-network

networks:
  remix-hono-network:
    external: true
