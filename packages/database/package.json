{
  "name": "@remix-hono-package/database",
  "version": "0.1.0",
  "scripts": {
    "build": "echo \"$npm_package_name: No build script specified\" && exit 0",
    "dev": "echo \"$npm_package_name: No dev script specified\" && exit 0"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=22.14",
    "npm": "Use pnpm package manager.",
    "yarn": "Use pnpm package manager.",
    "pnpm": ">=10.4.1"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
