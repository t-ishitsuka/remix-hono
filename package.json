{
  "name": "remix-hono",
  "version": "0.1.0-alpha",
  "description": "Remix(React-Router) + Hono(Express) = Remix-Hono",
  "author": "Aprire",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=22.14",
    "npm": "Use pnpm package manager.",
    "yarn": "Use pnpm package manager.",
    "pnpm": ">=10.4.1"
  }
}
